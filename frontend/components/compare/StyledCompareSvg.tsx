import styled from 'styled-components';

const StyledCompareSvg = styled.img`
  display: block;
  width: auto;
  height: min(20vw, 8rem);
  margin-bottom: 2rem;
  margin-left: auto;
  margin-right: auto;
`;

export default StyledCompareSvg;
